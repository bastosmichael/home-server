version: '3.8'

services:
  minecraft-server:
    image: itzg/minecraft-server:latest
    container_name: minecraft-server
    restart: unless-stopped
    ports:
      - "25565:25565"
    environment:
      - EULA=TRUE
      - VERSION=LATEST
    volumes:
      - minecraft_data:/data

volumes:
  minecraft_data:
